// To users of Windows, "Aux".js  could make a error. 
// Reserved name (really?). so, use Auxiliary.js

// import React from 'react';
// // We don't need to import 'react' cause we dont use any js here.

// Why to use it? just to wrap components to print in a return function 
const Aux = props => props.children;

export default Aux;